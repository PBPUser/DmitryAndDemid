using DmitryAndDemid.Launcher;

// The standalone configurator. It writes the same config.json the game reads; run it from the game's
// directory (or next to the game executable) so both see the same file.
new LauncherWindow().Open();
