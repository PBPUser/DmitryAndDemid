Create last spellcard in stage 2, which nikitab should spawns bullets from bottom (only if difficulty is hard or max), and create huge bullet that spawns yellow microbullets that rotates 180 degree for about 30 + 10 * (4-difficulty_level) ticks and always moves directed by their rotation

Create 3rd stage, add default chapter-placeholder,  then, in nikitab spell card (or non-spell if player plays easy/normal difficulty), in spellcard nikitab should appear rotating pizza from that circle from bullets (default and large) and it should be grouped into 2 on easy, 3 on normal/hard and 4 on max, which swaps their state to not attack player every 1/Group_count seconds, when they not attack player - they should be semitransparent with bullets randomly (use player position as seed) in it, that appears from sides, and laser when player plays hard/max difficulty, which rotates from center of pizza, nikitab should be invincible, spellcard background should use nikitab-spellcard-5.png and outlines of forks slowly float from the bottom side

Add dialog and first non-spell of "Dmitry", before dialog dmitry appears using strength animation and plays strength sound, in first non-spell

add tag to dialog where music switches to the boss

add tag that should appear boss name, and display his profile (profile-BOSSNAME.png) with rotating colored fork (profile should have json, and in json boss should have a color)

# after player kills toilet that spawns depended on network, it should spawn a piece of bomb

spawn collectables with slight velocity

add piece of heart when player successfully reaches end of non-spell/spell (no timeout, no death, no bomb), if player timed out - game should drop nothing, if player die or use bomb - game should take a star piece

add boss splash animation when they takes a spellcard, which should be their semi-transparent art appearing out-of-screen and move out-of-screen, but for dmitry it should use dmitry_top and dmitry_bottom pngs with slow scaling from center and pictures should slow move by y axis from each other and fade in and out (scale should start from 0.7~ times of screen and stop on 2~ times of screen), and use Easeing for scale, that speed-ups ending

